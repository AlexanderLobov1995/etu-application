app-popup
  .popup-container
    display flex
    position fixed
    top: 0
    left 0
    z-index: 1
    width 100vw
    height 100vh
    background-color: rgba(0,0,0,.5)
    .popup-content
      padding 0 18px 18px 18px
      height 500px
      margin auto
      background-color white
  & .hidden
      display none